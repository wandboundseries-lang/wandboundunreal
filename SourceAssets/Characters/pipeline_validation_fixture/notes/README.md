# Pipeline Validation Fixture

This bundle exists only to exercise the validation and dry-run command path. Its `.fbx` file is inert synthetic text and is never passed to an Unreal import factory.

Do not run Import or Reimport for this character ID. It is not a production character.
